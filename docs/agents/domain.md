# Domain documentation

Single-context backend repository. Read root `CONTEXT.md` and relevant
`docs/adr/` decisions when present; proceed if absent. For this phase the domain
vocabulary and decisions live in `docs/architecture.md`. Use those terms in
interfaces and tests. Flag conflicts with documented decisions explicitly.
The eventual `balboa-spa-card` is a separate repository, created in Phase 8.
