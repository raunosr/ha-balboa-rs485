# Issue tracker: GitHub

The user approved public HACS distribution on 2026-09-08. Earlier private history
is preserved separately as `raunosr/ha-balboa-rs485-private-archive`.
Repository: `raunosr/ha-balboa-rs485`. Use `gh` for requested issue operations.
Read with `gh issue view NUMBER --repo raunosr/ha-balboa-rs485 --json title,body,comments,labels`.
Use `--body-file` for multiline issue bodies. Issues and comments in the active
repository are public: never include credentials, home-network details or captures.
Do not infer permission to post issues/comments from a research or review request.
